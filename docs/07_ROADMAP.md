# Дорожная карта реализации — Радио «Толкай Вода»

10 этапов от пустого репозитория до прод-готовности. Каждый этап заканчивается
**демонстрируемым результатом**.

Ключевая идея: **MVP-first**. Этап 1 = живой радио-плеер на главной. Всё остальное
наслаивается без перепиливания базы.

> **Где смотреть прогресс**: текущий статус всех этапов — в [`STATUS.md`](STATUS.md).
> Внутри каждого этапа ниже — чекбоксы acceptance-пунктов. Закрытие этапа
> сопровождается git-тегом `stage-N-done`.

## Архитектурное решение (важный пункт ТЗ — нагрузка 100 юзеров на 2 ядра / 2 ГБ)

Плеер играет **случайный трек у каждого юзера** (не синхронный эфир), поэтому
HLS/FFmpeg-стрим **не нужен**. mp3 лежат на **S3 Beget** и раздаются через **CDN**
(`ceshocofureled.begetcdn.cloud`). Сайт играет mp3 **напрямую с CDN** — сервер не
трогает аудио-байты вообще. Всю аудио-полосу держит CDN, сервер (2 ядра / 2 ГБ)
обслуживает только лёгкий API блога/прогноза и Telegram-бота.

Стек: **Node.js + Fastify + better-sqlite3**, фронт — HTML/CSS/Vanilla JS,
reverse-proxy **Caddy** (авто-HTTPS), **docker-compose**, деплой **GitHub Actions**.

| # | Этап | Главный артефакт | Зависит от |
|---|---|---|---|
| 0 | Каркас репозитория | `npm run dev` поднимает Fastify, отдаёт пустую страницу | — |
| 1 | Радио-плеер | Кнопка играет случайный трек + анимация дорожки | 0 |
| 2 | S3/CDN треки | Плеер играет реальные 50 mp3 с CDN | 1 |
| 3 | Прогноз дня | Блок на главной с текстом+фото из БД | 0 |
| 4 | Блог | Сетка статей + страница статьи | 0 |
| 5 | Telegram-бот | Сообщение боту → контент на сайте | 3,4 |
| 6 | Контакты/футер | Кастомные иконки соцсетей, email | 1 |
| 7 | Адаптив | Мобильный вид (фон 9:16), оптимизация | 1-6 |
| 8 | Docker/CI/CD | Контейнеры локально + workflow деплоя | 1-7 |
| 9 | Прод-деплой | Сайт открывается на tolkay-voda.ru | 8 |

---

## Этап 0 — Каркас репозитория

**Цель**: пустой, но рабочий проект со всем для следующих этапов.

**Что делаем**:
- package.json, зависимости (fastify, @fastify/static, better-sqlite3, dotenv, pino)
- Fastify-сервер: раздача `public/`, healthcheck `/api/health`
- `.env.example`, конфиг, инициализация SQLite (`data/app.db`)
- `.gitignore`

**Критерии приёмки** (закрытие → tag `stage-0-done`):
- [x] `npm install && npm run dev` поднимает сервер на :3000 без ошибок
- [x] `GET /api/health` возвращает `{ok:true}`
- [x] Открытие `/` отдаёт страницу-заглушку из `public/index.html`
- [x] Файл `data/app.db` создаётся при старте

---

## Этап 1 — Радио-плеер (главная вертикаль)

**Цель**: живой плеер на главной по дизайн-макету.

**Что делаем**:
- Вёрстка главной: фон (озеро), лого по центру, заголовок «РАДИО ТОЛКАЙ ВОДА»
- Шрифты Oswald (заголовки) + Inter (текст), self-hosted woff2
- Кнопка «СЛУШАТЬ РАДИО» → случайный трек, по `ended` → следующий случайный
- Анимация звуковой дорожки (WebAudio AnalyserNode или CSS-fallback)
- Карточка «Сейчас играет»: обложка, название, исполнитель
- API `GET /api/tracks` (пока из локального манифеста-заглушки)

**Критерии приёмки** (закрытие → tag `stage-1-done`):
- [x] Главная визуально соответствует макету (фон, лого, заголовок, кнопка)
- [x] Клик по кнопке запускает случайный трек, звук играет
- [x] При воспроизведении видна анимация дорожки, на паузе — замирает
- [x] По окончании трека автоматически стартует следующий случайный
- [x] «Сейчас играет» показывает название текущего трека

---

## Этап 2 — S3/CDN: загрузка треков

**Цель**: плеер играет реальные песни клиента с CDN, не с сервера.

**Что делаем**:
- Скрипт распаковки `Радио Толкай Вода.zip`, нормализация имён (slug/uuid)
- Загрузка mp3 на S3 Beget (бакет `incomparable-darian`)
- Генерация `tracks.json` (название из тега/имени файла + CDN-URL)
- `GET /api/tracks` отдаёт реальный манифест

**Критерии приёмки** (закрытие → tag `stage-2-done`):
- [x] 50 mp3 доступны по CDN-URL (HTTP 200, audio/mpeg)
- [x] `tracks.json` содержит все треки с корректными названиями
- [x] Плеер на главной играет реальные треки с CDN
- [x] Аудио грузится с CDN-домена (проверка в Network), не с сервера

---

## Этап 3 — Прогноз дня

**Цель**: блок «Прогноз дня» на главной с данными из БД.

**Что делаем**:
- Таблица `forecast` (дата, заголовок, тело, краткая выжимка, фото-URL)
- API `GET /api/forecast/today`, `GET /api/forecast/:id`
- Вёрстка блока по макету + страница/модалка «Читать полный прогноз»

**Критерии приёмки** (закрытие → tag `stage-3-done`):
- [x] Блок «Прогноз дня» показывает актуальную запись из БД (текст + фото)
- [x] «Читать полный прогноз» открывает полный текст
- [x] Если прогноза за сегодня нет — показывается последний доступный

---

## Этап 4 — Блог

**Цель**: раздел блога сеткой + страница статьи.

**Что делаем**:
- Таблица `posts` (заголовок, тело, фото-URL, дата, slug)
- API `GET /api/posts?page=`, `GET /api/posts/:slug`
- Сетка карточек + страница статьи

**Критерии приёмки** (закрытие → tag `stage-4-done`):
- [x] Раздел блога показывает карточки статей сеткой
- [x] Клик открывает полную статью (текст + фото)
- [x] Пагинация работает при >N статей

---

## Этап 5 — Telegram-бот контента

**Цель**: контент попадает на сайт из Telegram.

**Что делаем**:
- Бот (grammY/Telegraf, long-polling), whitelist разрешённых ID
- Личка: фото+текст. Подпись с «Прогноз» → обновляет прогноз дня; иначе → пост блога
- Загрузка фото из Telegram на S3, сохранение записи в SQLite
- Логирование pino (на русском: дата, username, id, действие)

**Критерии приёмки** (закрытие → tag `stage-5-done`):
- [x] Прогноз дня через бота (FSM + ИИ-выжимка) → обновляет блок на главной
- [x] Статья через бота (FSM: заголовок → текст → фото) → появляется в ленте блога
- [x] Фото из Telegram доступно по CDN-URL на сайте
- [x] Сообщение от не-whitelist ID игнорируется
- [x] Каждое действие залогировано на русском

> Итог: вместо ключевых слов — пошаговый сценарий с инлайн-кнопками. Текст
> прогноза причёсывает OpenAI gpt-4o-mini в структуру (лунный день, вводка,
> Вода/Цвет/Еда/Совет дня, полный текст). Блог — горизонтальная лента.

---

## Этап 6 — Контакты, футер, соцсети

**Цель**: связь без сбора ПД (требование РКН из переписки).

**Что делаем**:
- Кастомные SVG-иконки (Telegram, YouTube) — без эмодзи
- Email `radio@tolkay-voda.ru`, ссылки на @klanTolkayvoda и YouTube
- Футер: © Sellme Marketing, ссылка на политику конфиденциальности
- Страница политики конфиденциальности

**Критерии приёмки** (закрытие → tag `stage-6-done`):
- [x] Кастомные иконки соцсетей (не эмодзи) в секции «Связь с кланом», ведут на нужные ссылки (в футере не дублируются — только копирайт и политика)
- [x] Email кликабелен (mailto)
- [x] Страница политики конфиденциальности открывается
- [x] Форм сбора персональных данных нет

> Дополнительно по просьбе клиента: добавлена шапка сайта (sticky-навбар с
> навигацией Радио/Прогноз/Блог/Связь, прозрачный поверх hero → тёмный при скролле).

---

## Этап 7 — Адаптив и оптимизация

**Цель**: корректный мобильный вид и быстрая загрузка.

**Что делаем**:
- Мобильный фон 9:16, адаптив всех блоков (плеер, прогноз, блог, футер)
- Оптимизация изображений (webp), lazy-load, preload шрифтов
- Проверка перф (Lighthouse mobile)

**Критерии приёмки** (закрытие → tag `stage-7-done`):
- [x] На мобильном фон 9:16, всё читаемо и кликабельно
- [x] Нет горизонтального скролла на 360px (и на 320px)
- [x] Lighthouse Performance (mobile) ≥ 85 → 91–95 (два прогона)

---

## Этап 8 — Docker, Caddy, CI/CD

**Цель**: воспроизводимый деплой контейнерами.

**Что делаем**:
- Dockerfile (node:slim), docker-compose (app + caddy)
- Caddyfile: авто-HTTPS для tolkay-voda.ru, проксирование на app
- GitHub Actions: build + deploy по SSH на сервер при пуше в master

**Критерии приёмки** (закрытие → tag `stage-8-done`):
- [x] `docker compose up` локально поднимает сайт целиком (app healthy + caddy)
- [x] Caddy отдаёт сайт по HTTP локально (HTTP 200 через `via: Caddy`, ассеты/страницы 200)
- [x] Workflow деплоя создан и провалидирован (compose config OK, структура job/secrets) — живой прогон по push в `master` выполняется на этапе 9 после настройки сервера и secrets

---

## Этап 9 — Прод-деплой (okdeploy)

**Цель**: сайт в проде на домене клиента.

**Что делаем**:
- Деплой на сервер 159.194.201.64
- DNS tolkay-voda.ru → сервер, авто-HTTPS
- Проверка, что n8n на поддомене не затронут
- Финальная приёмка с клиентом

**Критерии приёмки** (закрытие → tag `stage-9-done`):
- [x] https://tolkay-voda.ru открывается, радио играет (LE-сертификат, трек с CDN, HTTP→HTTPS 308)
- [x] Прогноз дня и блог обновляются из Telegram в проде (бот @tolkayvodabot, long-polling, принимает команды админа)
- [x] n8n.tolkay-voda.ru продолжает работать (200, свой сервер 31.207.75.17 — не затронут)
- [x] Сайт стабилен при ~100 одновременных слушателях (аудио идёт с CDN мимо сервера)

---

# Майлстоун 2 — пост-MVP правки (этапы 10–12)

Проект сдан и принят (этапы 0–9, 2026-06-17). Клиент попросил три доработки.
Этапы **независимы** друг от друга, порядок — по риску/инфраструктуре.

| # | Этап | Главный артефакт | Зависит от |
|---|---|---|---|
| 10 | Редактирование контента | В боте можно править/удалять опубликованные прогнозы и статьи | 5 |
| 11 | Живой эфир (Icecast) | Единый поток на всех, вход «как на радио» (с середины) | 2,8 |
| 12 | SEO под капотом | robots.txt, sitemap, OG, серверные мета статей, JSON-LD | 4,8 |

> **Изменение архитектурного решения по аудио.** Ранее (раздел выше) было
> зафиксировано «синхронный эфир не нужен, HLS/стрим не нужен — каждый юзер играет
> свой случайный трек с CDN». Клиент изменил требование: нужен **единый эфир как на
> радио** (один поток на всех, вход с текущей точки). Решение по стеку (см. этап 11):
> **Icecast + Liquidsoap**, источник треков — **с CDN** (исходники и их хранение
> остаются на CDN, сервер не держит 50 mp3 на диске), один энкод 128k MP3 → раздача
> через Icecast. Под 2 ядра / 2 ГБ это лёгкая нагрузка (один декод + один энкод +
> fan-out байтов). Раздача слушателям идёт с аплинка сервера; для нишевой аудитории
> (десятки–сотни) это незаметно, апгрейд на HLS+CDN-релей — задел на будущее, если
> аудитория вырастет до тысяч. Старый «джукбокс» остаётся **фолбэком** при недоступном
> потоке.

---

## Этап 10 — Редактирование контента через бота

**Цель**: править и удалять уже опубликованные прогнозы и статьи, а не только создавать.

**Что делаем**:
- `src/bot/store.js`: чтение списков (`listRecentForecasts`, `listRecentPosts`),
  выборка по id, точечное обновление полей (`updatePostFields`, `updateForecastFields`),
  удаление (`deletePost`, `deleteForecast`).
- Бот: вход «Редактировать» (в `/start` + команда `/edit`) → выбор типа → список
  последних N записей инлайн-кнопками → выбор записи → меню полей
  (заголовок / текст / фото / удалить) → ввод нового значения → сохранение.
- Прогноз: точечная правка полей; при правке полного текста — опц. повторный прогон ИИ.
- Замена фото: новое фото Telegram → S3 → CDN, обновление `image_url`.
- Логи pino на русском (что, какая запись, кто правил/удалил).

**Критерии приёмки** (закрытие → tag `stage-10-done`):
- [x] В боте есть вход «Редактировать» (через `/start` и `/edit`) со списком последних прогнозов/статей
- [x] Можно выбрать статью и изменить заголовок / текст / фото — правка видна на сайте
- [x] Можно выбрать прогноз и изменить текст / структурные поля / фото — правка видна на главной
- [x] Можно удалить статью и прогноз — запись пропадает с сайта
- [x] Каждое редактирование и удаление залогировано на русском (id записи, поле)
- [x] Доступ к редактированию — только whitelist (как при создании)

> Дополнительно: меню команд бота слева от поля ввода (`setMyCommands`:
> start / edit / help / cancel). Проверено клиентом в проде (@tolkayvodabot).

---

## Этап 11 — Живой эфир (Icecast + Liquidsoap)

**Цель**: единый синхронный поток на всех слушателей, вход с текущей («live») точки — как на настоящем радио.

**Архитектура**: Liquidsoap тянет треки **с CDN** (плейлист CDN-URL из `tracks.json`),
shuffle + crossfade + метаданные → один **128k MP3** → **Icecast** раздаёт всем.
Caddy проксирует `/stream` и now-playing. Джукбокс (`radio.js`) — фолбэк.

**Что делаем**:
- `docker-compose.yml`: сервисы `icecast` + `liquidsoap` (общий конфиг, source-пароль из `.env`).
- Liquidsoap-скрипт: плейлист CDN-URL, `randomize`, `crossfade`, метаданные now-playing,
  `output.icecast` (mp3 128k); фолбэк-тишина/джингл если плейлист пуст.
- Скрипт генерации плейлиста из `tracks.json` (CDN-URL списком).
- Caddy: `reverse_proxy /stream` → `icecast:8000`, эндпоинт now-playing.
- API now-playing: лёгкий JSON `{title, artist}` (проксирование/парсинг Icecast `status-json.xsl`), с кешем.
- `radio.js`: основной режим — `<audio src="/stream">`, «сейчас играет» из now-playing (поллинг),
  визуализатор на потоке (same-origin → анализатор работает); при недоступном `/stream` —
  автоматический фолбэк на джукбокс.
- Проверка ресурсов (один энкод): CPU/RAM в пределах 2 ядра / 2 ГБ.

**Критерии приёмки** (закрытие → tag `stage-11-done`):
- [x] Два устройства слышат одно и то же в один момент (синхронный эфир)
- [x] Новый слушатель входит в текущую точку трека, а не с начала
- [x] «Сейчас играет» показывает данные потока (у всех треков один бренд-тег; битые ICY-метаданные отфильтрованы → показываем «Радио Толкай Вода»)
- [x] При недоступном `/stream` плеер падает на джукбокс (сайт не «немеет»)
- [x] Источник треков — CDN; 50 mp3 на сервере не хранятся (диск чист)
- [x] `docker compose up` поднимает icecast+liquidsoap, `/stream` отдаёт `audio/mpeg` (проверено на сервере)
- [x] На проде поток стабилен; один 128k-энкод (liquidsoap ~6.7% CPU / 138 МБ, icecast ~2 МБ), запас RAM ~1.2 ГБ

> Стоимость CDN: эфир тянет один источник 24/7 (~80 ГБ/мес ≈ 48 ₽/мес), не зависит
> от числа слушателей (их раздаёт Icecast с сервера, не CDN). Кратно дешевле джукбокса.

---

## Этап 12 — SEO под капотом (техническое)

**Цель**: невидимая глазу оптимизация для поисковиков и шаринга (на странице визуально
ничего не меняется — весь SEO «под капотом»).

**Что делаем**:
- `public/robots.txt` (allow, ссылка на sitemap).
- Динамический `GET /sitemap.xml` из БД: главная, `/privacy.html`, все опубликованные статьи.
- Open Graph + Twitter-карточки на `index.html` и `privacy.html` (заголовок, описание, og:image).
- Серверный маршрут ЧПУ `GET /blog/:slug` → отдаёт `post.html` с **подставленными**
  серверными мета-тегами статьи (`<title>`, `description`, `og:title/description/image`,
  `canonical`) из БД. Старый `/post.html?slug=` продолжает работать / редиректит на ЧПУ.
- JSON-LD: `Organization`/`RadioStation` на главной, `Article` на статьях.
- `canonical` и `og:locale` на всех публичных страницах.

**Критерии приёмки** (закрытие → tag `stage-12-done`):
- [x] `/robots.txt` отдаётся (200) и указывает на sitemap
- [x] `/sitemap.xml` отдаётся и содержит главную + все опубликованные статьи
- [x] При шаринге подтягивается превью-карточка (заголовок + описание) — проверено в Telegram
- [x] Каждая статья доступна по ЧПУ `/blog/:slug` с уникальными серверными мета-тегами
- [x] На главной и статьях присутствует валидный JSON-LD (RadioStation / Article)
- [x] `canonical` проставлен на всех публичных страницах

---

# Майлстоун 3 — Управление фонотекой через бота

> **Контекст.** Сейчас 50 песен эфира «прошиты» в файл `public/assets/tracks.json`
> (внутри docker-образа) и в `docker/liquidsoap/playlist.m3u` (примонтирован к
> Liquidsoap **только на чтение** из репозитория). Менять фонотеку можно лишь
> правкой кода и передеплоем. Цель майлстоуна — отдать управление песнями клиенту
> через Telegram-бота (как уже сделано с прогнозами и статьями на этапе 10).

> **Архитектурное решение.** Переносим список треков из файла в БД (SQLite,
> постоянный том `app-data` — переживает передеплои). Плейлист для эфира
> генерируется приложением из БД в **общий том** между контейнерами `app` и
> `liquidsoap`; Liquidsoap уже читает плейлист с `reload_mode="watch"`, поэтому
> подхватывает изменения **на лету, без рестарта**. Загрузка mp3 в S3 и запись в
> БД переиспользуют готовые `s3.js` / паттерн `media.js` с этапа 5. Имя файла в S3 —
> ASCII-слаг из названия (через `lib-tracks.slugify`), чтобы продолжал работать
> резолв русского названия в «сейчас играет» (фикс `fix(radio)` от 2026-06-22).

---

## Этап 13 — Добавление песен через бота

**Цель**: клиент присылает боту mp3 + название — песня появляется в эфире и на
сайте без передеплоя.

**Что делаем**:
- Таблица `tracks` в SQLite (`position`, `title`, `artist`, `key`, `url`, `cover`,
  `created_at`); сид-миграция переносит текущие 50 из `tracks.json`.
- `/api/tracks` и резолв «сейчас играет» (`/api/stream/now`) читают из БД.
- Генерация `playlist.m3u` из БД в **общий том**; `docker-compose.yml` — общий
  том для `app` + `liquidsoap`, `radio.liq` → новый путь к плейлисту.
- `media.js`: `uploadTelegramAudio` (mp3 Telegram → S3, ключ = слаг названия).
- Бот: «Добавить песню» → аудио + название → S3 → запись в БД → перегенерация
  плейлиста → подтверждение. Доступ — whitelist. Логи pino на русском.

**Критерии приёмки** (закрытие → tag `stage-13-done`):
- [x] Текущие 50 треков перенесены в БД; сайт и эфир работают как раньше (нет регресса)
- [x] В боте есть «Добавить песню» (доступ — только whitelist)
- [x] Отправка mp3 + названия → файл лёг в S3, запись появилась в БД
- [x] Новая песня видна в списке на сайте (`/api/tracks`) и попадает в ротацию эфира без передеплоя
- [x] «Сейчас играет» показывает русское название новой песни
- [x] Добавление залогировано на русском (название, кто добавил)

---

## Этап 14 — Удаление песен через бота

**Цель**: клиент убирает старую песню из эфира и с сайта одной командой.

**Что делаем**:
- `store.js`: `listTracks`, `getTrackById`, `deleteTrack` (+ опц. удаление объекта в S3).
- Бот: «Удалить песню» → список треков инлайн-кнопками → выбор → подтверждение →
  удаление записи → перегенерация плейлиста.
- Защита: нельзя удалить последний трек (иначе эфир уйдёт в тишину).
- Доступ — whitelist. Логи pino на русском.

**Критерии приёмки** (закрытие → tag `stage-14-done`):
- [x] В боте есть «Удалить песню» со списком треков (доступ — только whitelist)
- [x] Выбор трека + подтверждение → запись пропадает из БД
- [x] Удалённая песня исчезает из списка на сайте и из ротации эфира без передеплоя
- [x] Нельзя удалить последний оставшийся трек (защита от пустого эфира)
- [x] Удаление залогировано на русском (название, кто удалил)

---

# Майлстоун 4 — Переезд на новый сервер + рекламные баннеры

> **Контекст.** Клиент хочет (1) съехать на более дешёвый сервер, оставив S3 и CDN
> на Beget как есть, и (2) поставить под плеером два баннерных места, чтобы окупать
> обслуживание. Этапы независимы — порядок можно менять, если переезд затормозит
> оплата сервера или DNS.

> **Замеры перед планированием (2026-09-04, сняты вживую).**
> Новый сервер `89.125.121.199`: Ubuntu 24.04.4, **1 ядро** Xeon E5-2673 v4 @ 2.3 ГГц
> (KVM, steal 0%), 1967 МБ RAM, **swap 0**, диск 96 ГБ, Docker не установлен.
> Текущий прод (`docker stats`): liquidsoap 6.66% CPU / 639 МБ, bot 168 МБ, app 87 МБ,
> caddy 24 МБ, icecast 1.6 МБ — итого **~920 МБ и ~7% одного ядра**. Контейнеры живут
> 2 месяца, 0 рестартов, OOM не было; swap 2 ГБ занят на 237 МБ.
> Сеть нового сервера: порт 80 снаружи открыт (проверено извне), 443 не фильтруется,
> CDN и S3 Beget отвечают 200 на скорости 11.5 МБ/с, пять range-запросов подряд —
> **ни одной 523**, Docker Hub и GitHub доступны.
> **Вывод:** одного ядра хватает с 14-кратным запасом, память сходится ровно, но
> **swap 2 ГБ обязателен** — без него стек упрётся в потолок 2 ГБ.

---

## Этап 15 — Переезд на сервер 89.125.121.199

**Цель**: сайт, эфир и бот работают на новом сервере, старый остаётся холодным
откатом; S3, CDN и домен на Beget не трогаем.

**Что делаем**:
- Подготовка сервера: swap 2 ГБ, Docker, git, часовой пояс.
- Клон `master` в `/opt/tolkay_voda`, перенос `.env` (секреты S3/BOT/OPENAI/ICECAST).
- Перенос данных: том `app-data` (SQLite — прогнозы, статьи, фонотека) со старого
  сервера. Кэш mp3 **не копируем** — `syncAudioCache` сам скачает недостающие треки
  из S3 при старте (~250 МБ на скорости 12.5 МБ/с).
- DNS на Beget: A-запись apex `tolkay-voda.ru` → новый IP. `n8n.tolkay-voda.ru`
  (31.207.75.17) не трогаем.
- **Остановить бота на старом сервере до старта нового** — два long-polling
  процесса на одном токене дают конфликт 409, бот начнёт терять сообщения.
- GitHub Secrets: `SSH_HOST` / `SSH_USER` / `SSH_KEY` под новый сервер, deploy-ключ.
- Старый сервер не выключаем 3–7 дней.

**Критерии приёмки** (закрытие → tag `stage-15-done`):
- [x] На сервере активен swap 2 ГБ (`swapon --show`), Docker установлен
- [x] `docker compose up -d --build` поднимает app (healthy) + caddy + icecast + liquidsoap + bot
- [x] База перенесена: число статей, прогнозов и треков совпадает со старым сервером
- [x] Кэш mp3 восстановился сам из S3 (файлы в томе `audio-cache`, вручную не копировали)
- [x] `https://tolkay-voda.ru` отдаёт 200 с валидным сертификатом (перенесён со
      старого сервера, а не выпущен заново), `http` → 308 на `https`
- [x] `n8n.tolkay-voda.ru` продолжает открываться (не задет переездом)
- [x] `/stream` отдаёт `audio/mpeg` и **реально звучит** (проверка `volumedetect`, не тишина)
- [x] «Сейчас играет» показывает русское название трека
- [x] Бот @tolkayvodabot отвечает на `/start` и публикует контент; на старом сервере бот остановлен
- [x] Пуш в `master` деплоит на новый сервер (GitHub Actions зелёный)
- [x] Клиент открыл сайт и подтвердил, что радио играет

### Журнал этапа 15

**2026-09-10, подготовка нового сервера завершена (проверено по IP, DNS ещё на старом).**

Сделано:
- swap 2 ГБ (`/swapfile`, `vm.swappiness=10`, в `/etc/fstab`), TZ Europe/Moscow,
  Docker 29.8.0 + Compose v5.5.1.
- Клон `master` в `/opt/tolkay_voda` — коммит `1e2fa47`, тот же, что на проде.
- `.env` скопирован со старого сервера побайтово (sha256 совпал). `COMPOSE_PROFILES`
  временно пуст — бот не поднимаем, пока живёт бот на старом сервере (иначе 409).
- База перенесена снимком `VACUUM INTO` после `wal_checkpoint(TRUNCATE)`. Важно:
  на старом сервере в WAL лежало 486 КБ незасинхронизированных данных — простое
  копирование `app.db` их бы потеряло. sha256 снимка совпал на обоих серверах.
- Временный `docker-compose.override.yml` держит Caddy на голом `:80`: пока домен
  смотрит на старый сервер, ACME-проверка Let's Encrypt уходила бы туда и жгла лимиты.
  **Удалить при переключении DNS.**
- Сгенерирован deploy-ключ `/root/.ssh/github_actions` (ed25519), публичная часть
  уже в `authorized_keys` нового сервера.

Замеры:
- Первая сборка образа без кэша слоёв — около 30 минут (одно ядро; часть времени
  две сборки конкурировали за ядро, чистая была бы заметно быстрее). `npm ci` сам
  по себе — 16.5 минут. Последующие деплои переиспользуют слои, пока не менялся
  `package.json`.
- Кэш эфира восстановился сам: **66/66 файлов, 253 МБ, ошибок 0**, вручную не копировали.
  Столько же, сколько на старом сервере.
- База на новом сервере: forecast 19, posts 2, tracks 66 — совпадает со старым.
- Эфир звучит: `volumedetect` за 8 секунд даёт mean −15.4 dB, max −3.2 dB (не тишина).
- `/api/stream/now` отдаёт русское название («Бусибудь!», «Клан Толкай Вода»).
- Снаружи по IP: `http://89.125.121.199/` → 200 (31 КБ), с заголовком
  `Host: tolkay-voda.ru` → 200, `/stream` → `audio/mpeg`.
- Ресурсы под нагрузкой: caddy 50 МБ, liquidsoap 244 МБ, app 91 МБ, icecast 2.4 МБ —
  677 МБ занято всего, swap тронут на 1 МБ, CPU ~9% одного ядра.

**2026-09-10, боевая конфигурация на новом сервере (DNS ещё не переключён).**

- Сертификат Let's Encrypt (`tolkay-voda.ru`, годен до 14.11.2026) вместе с ACME-аккаунтом
  скопирован со старого сервера в том `caddy_data` нового. Caddy поднял его из хранилища
  и **не делал ни одного запроса к Let's Encrypt** — значит переключение DNS пройдёт без
  разрыва HTTPS и без риска упереться в лимиты выпуска.
- Бот на старом сервере остановлен (`docker compose stop bot`), на новом поднят.
  Все 5 контейнеров работают.
- Проверено через `--resolve` на новый IP: `https://tolkay-voda.ru/` → 200 с валидным
  сертификатом, `http://` → 308, `/stream` → `audio/mpeg`, `/api/stream/now` отдаёт
  русское название.

### Блокер, найденный при переезде: провайдер нового сервера режет Telegram

Новый сервер — **AS33842 Citytelecom LLC, Москва** (не Beget). Старый сервер —
**AS198610 Beget LLC, Санкт-Петербург**. То есть клиент съезжает *с* Beget, а не *на* Beget,
как звучала задача.

С нового сервера TCP:443 не проходит почти ни на один адрес Telegram: из проверенных
`149.154.166.110`, `149.154.175.50`, `149.154.164.5`, `149.154.171.5`, `91.108.56.130`
не отвечает ни один, хотя ICMP до них идёт с потерями 0% — значит фильтр по IP+порт,
маршрут в порядке. DNS отдаёт `149.154.166.110`, поэтому бот при старте просто висел
на `ConnectTimeout` и молчал (логи пустые, контейнер при этом «Up» — тихий отказ).

Проходит **единственный адрес `149.154.167.220`**, и через него bot API работает
полностью: `getMe`, `getUpdates`, `/file`. Со старого сервера (Beget) доступны и
`166.110`, и `175.50` — там блокировки нет.

**Временный обход:** `docker-compose.override.yml` на новом сервере (вне git —
он верен только для этого провайдера) привязывает `api.telegram.org` к `149.154.167.220`
через `extra_hosts` для контейнеров `bot` и `app`. Бот запустился и работает.

**Чем это плохо:** вся работа бота висит на одном IP, который провайдер сегодня не режет,
а завтра может. Отказ будет тихим — контейнер останется «Up», а бот замолчит.
Это довод против этого сервера, и его стоит донести клиенту до окончательного переезда.

**2026-09-10, 13:17 МСК — DNS переключён, сайт живёт на новом сервере.**

- Все шесть авторитативных NS Beget (`ns1/ns2.beget.ru`, `.pro`, `.com`) согласованно
  отдают `89.125.121.199`. Публичные резолверы разъехались за минуты; отдельные
  edge-узлы доживали кэш до 10 минут — в это время часть посетителей шла на старый
  сервер, часть на новый, оба отвечали корректно. Разрыва не было.
- `n8n.tolkay-voda.ru` → `31.207.75.17` на всех резолверах, не задет.
- **Caddy не сделал ни одного запроса к Let's Encrypt** — перенесённый сертификат
  подхватился из хранилища. Ставка на копирование `caddy_data` оправдалась.
- Бот ответил на `/start` живому пользователю: `Вход @Polarasing (id:2042819654, Kira)
  -> /start` → `Бот -> @Polarasing: Что добавить на сайт?`. Привязка к `149.154.167.220`
  работает.
- Эфир на новом сервере звучит: `volumedetect` mean −18.2 dB, max −5.0 dB.
- Прогон скрипта автодеплоя (тот же, что в Actions, через выданный deploy-ключ):
  **22 секунды**, `docker-compose.override.yml` пережил `git reset --hard`, все
  контейнеры поднялись.
- Ресурсы под реальным трафиком: caddy 10 МБ, bot 39 МБ, app 44 МБ, liquidsoap 234 МБ,
  icecast 2.5 МБ — 684 МБ занято всего, swap тронут на 1 МБ, load average 0.03.
  Рестартов 0 у всех пяти контейнеров, ошибок уровня error/fatal в логах нет.

Осталось (порядок обязателен, шаги 2–5 идут одним окном за ~10 минут):
**2026-09-10, этап закрыт.**

- **Автодеплой проверен по-настоящему**: мёрж `dev` → `master`, прогон GitHub Actions
  №21 зелёный, на сервере встал коммит `a78d69c`. Эфир при деплое **не прерывался** —
  `liquidsoap` и `icecast` не пересоздавались, у них аптайм 2 часа против 57 секунд
  у `app`. `docker-compose.override.yml` пережил `git reset --hard`, как и рассчитывалось.
- Сайт проверен пользователем, работает.
- **Ревизия старого сервера перед его отключением**: в `/opt` ничего, кроме проекта;
  репозиторий чист, из неотслеживаемого только `.env` (перенесён); все пять томов
  учтены; своих cron-задач и systemd-юнитов нет; других сайтов и сервисов на машине нет.
  Контрольный снимок базы дал sha256 `5463736…` — **побайтово тот же**, что на новом
  сервере. Не перенесённого не осталось.
- Локальная копия базы на всякий случай: `сайт_вода/backup/app-db-2026-09-10.db`
  (без секретов — `.env` живёт на новом сервере).

> **Окно отката закрывается через 2 дня**, а не через неделю: у клиента заканчивается
> оплата старого сервера. После этого откатываться будет некуда. Риск принят
> осознанно — переезд проверен целиком, включая автодеплой, и на старом сервере
> не осталось ничего своего.

## Этап 16 — Рекламные баннеры под плеером (витрина)

**Цель**: под плеером два баннерных места. Этот этап делает только показ:
данные, API и вёрстку. Управление баннерами живёт в панели этапа 17 — в боте
его не будет, бот выводится из проекта.

**Решения по брифу клиента** (ответы от 2026-09-04):
- Ровно **два фиксированных слота**, без ротации и без пула рекламодателей.
- **Только главная**, под плеером. В блоге и статьях баннеров нет («как можно проще»).
- **Без статистики** показов и кликов.
- **Без срока размещения** — баннер висит, пока его не заменят.
- **Без блока «место для вашей рекламы»** и без формы заявки.
- Картинки готовит клиент: **16:9, 1200×675, JPG/PNG до 500 КБ**.
- Слова «Реклама», данных рекламодателя и токенов ОРД на сайте не будет — решение клиента.

**Что делаем**:
- Таблица `ads` в SQLite (том `app-data`): `slot` (1 или 2, первичный ключ),
  `image_key`, `image_url`, `link_url`, `button_text` (может быть пустым), `updated_at`.
- `GET /api/ads` — отдаёт заполненные слоты.
- Блок на главной **между «Сейчас играет» и «Прогнозом дня»**: две карточки в ряд,
  на мобиле — в столбик. Карточка кликабельна целиком, ссылка открывается в новой
  вкладке с `rel="noopener nofollow sponsored"` (чтобы платные ссылки не тянули вниз
  SEO, выстроенное на этапе 12).
- Пустой слот не отрисовывается; оба пустых — блок скрыт целиком.
- Валидация ссылки (только `http`/`https`), `aspect-ratio: 16/9` + `loading=lazy`,
  чтобы блок не дёргал вёрстку при загрузке (как сделано на этапе 7).

**Критерии приёмки** (закрытие → tag `stage-16-done`):
- [x] Под плеером на главной два баннерных места; на мобиле встают в столбик
- [x] Заполненный слот в `ads` показывается на сайте, `GET /api/ads` отдаёт его данные
- [x] Клик по баннеру открывает ссылку рекламодателя в новой вкладке
- [x] Замена картинки и ссылки в `ads` меняет сайт **без передеплоя**
- [x] Пустой слот на сайте не показывается
- [x] Оба слота пусты → блок скрыт, вёрстка страницы не ломается
- [x] Ссылка не по схеме `http`/`https` отклоняется на уровне API
- [x] Блок не сдвигает страницу при загрузке (нет скачка вёрстки)
- [x] Ссылка помечена `rel="noopener nofollow sponsored"`
- [x] То же проверено на проде (Actions №22, коммит `3180a57`, 2026-09-11)

> На этом этапе слоты заполняются напрямую в базе — интерфейса ещё нет.
> «Клиент сам поставил свой баннер» проверяется в приёмке этапа 17, когда появится
> панель. Этап 16 — предпосылка для 17, отдельной ценности клиенту не даёт, поэтому
> имеет смысл делать их подряд и показывать клиенту уже результат 17.

> **Открытый вопрос — кнопка внутри баннера.** Клиент написал «не будет кнопки, просто
> две картинки», но на присланном макете у обеих карточек есть золотая кнопка
> («Перейти на сайт», «Наш RUTUBE канал»), и она нарисована в стиле сайта, а не внутри
> картинки. Предлагаю сделать текст кнопки **необязательным полем**: задал текст —
> под картинкой рисуется золотая кнопка сайта; не задал — кликабельна вся картинка.
> Это закрывает оба прочтения и стоит одного лишнего поля в форме.

> **Второе расхождение:** макет нарисован карточками почти квадратными (~1:1), а
> согласованный с клиентом формат — 16:9 (1200×675). Оставляю 16:9, как договорились:
> клиент уже готовит картинки под него. Иначе его же макеты будут обрезаться.

---

### Журнал этапа 16

**2026-09-10, витрина написана и проверена локально. В прод не выкладывалась.**

Сделано:
- Таблица `ads` (миграция в `src/lib/db.js`): `slot` — первичный ключ, строки нет →
  слот пуст. `image_key` заведён под замену объекта в S3 из панели этапа 17.
- `src/lib/ads-store.js` — чтение и запись слотов. Ссылки принимаются только по
  схемам `http`/`https`. Проверка стоит **на обеих сторонах**: при записи отбивается
  с понятным кодом ошибки, при чтении негодная строка молча не попадает на сайт.
  Второе — не паранойя: до появления панели база правится руками, и в `href`
  попадает чужой URL.
- `GET /api/ads` (кэш 60 с) и `scripts/set-ad.js` (`npm run ads`) — заполнение слотов
  до появления панели, в том числе на сервере: `docker exec tolkay-app node scripts/set-ad.js list`.
- Блок на главной между «Сейчас играет» и «Прогнозом дня». Карточка кликабельна
  целиком, `rel="noopener noreferrer nofollow sponsored"`. Текст кнопки необязателен:
  задан — под картинкой золотая кнопка сайта, пуст — кликабельна вся картинка.
- `align-items: start` у сетки: карточка без кнопки не тянется до высоты соседней
  с кнопкой и не показывает пустую белую полосу снизу.

**Главная переехала со статики на свой маршрут** (`src/routes/home.js`). Изначально
блок рисовался на клиенте по ответу `/api/ads` — и, как показал замер, толкал вниз
прогноз и блог. CLS по 5 прогонов на ширину, свежий контекст браузера:

| | 390 px | 1280 px |
|---|---|---|
| блок рисует клиент | 0.0523 | 0.0912 |
| блок отдаёт сервер | 0.0004 | 0.0001 |

Сервер подставляет готовые карточки в разметку и кэширует результат, пересобирая
его только при смене баннеров. Статике выключен `index`, иначе маршрут `/`
дублируется. `public/js/ads.js` остался для страниц-фолбэков и пропускает работу,
если сервер уже отрисовал блок. Побочно: баннеры видны и без JavaScript.

Проверено (локально, временная БД, сервер ни разу не перезапускался при смене слотов):
- два слота на десктопе в ряд (433 px каждый), на 390 px — в столбик; пропорция
  картинки 1.78 на обеих ширинах, горизонтального скролла нет;
- клик по баннеру открыл ссылку в новой вкладке;
- один слот заполнен → второй не рисуется, карточка центрируется;
- оба слота пусты → блок скрыт, прогноз встаёт ровно на его место, вёрстка цела;
- `javascript:`-ссылка отбита при записи и отсеяна при чтении: строка, вставленная
  в базу напрямую мимо валидации, на сайт не попала, соседний нормальный слот остался;
- смена картинки и ссылки в базе меняла сайт без перезапуска приложения.

**2026-09-11, выложено в прод, этап закрыт — tag `stage-16-done` на `3180a57`.**

Мерж в `master`, прогон Actions №22 зелёный. Проверено на боевом образе через
`fastify.inject()` с временной БД в `/tmp` (порт не слушали, боевую базу, плейлист
и кэш эфира не трогали): `GET /api/ads` отдаёт оба слота, в HTML главной приходит
готовый блок с `data-ssr="1"` и `rel="noopener noreferrer nofollow sponsored"`,
подпись кнопки есть только у первого слота, у второго `alt="Баннер партнёра"`,
`javascript:`-ссылка отбита с кодом `bad_link_url`, пустые слоты дают скрытый блок.
Живые ответы через Caddy: главная 200 (`cache-control: public, max-age=60` — значит
отдаёт новый маршрут, а не статика), `/api/ads` пустой массив, эфир 200 `audio/mpeg`.

Деплой **не прервал эфир**: пересозданы только `app`, `bot` и `caddy`, у
`liquidsoap` и `icecast` аптайм 22 часа против 13 минут у остальных.

> **Слоты в проде пусты — баннеров на сайте не видно, и это штатно.** Картинки и
> ссылки даёт клиент; ставить их он будет сам из панели этапа 17. До неё слот
> ставится командой на сервере:
> `docker exec tolkay-app node scripts/set-ad.js set 1 <url-картинки> <url-ссылки> ["текст кнопки"]`.
> Проверка «клиент сам поставил свой баннер» остаётся критерием приёмки этапа 17.

> **Кнопка внутри баннера — вопрос закрыт компромиссом.** Слова клиента («не будет
> кнопки, просто две картинки») и его же макет (у обеих карточек золотая кнопка)
> расходились. Сделано необязательным полем — работают оба прочтения, спрашивать
> нечего. Отдельно подтверждать у клиента не требуется.

---

## Этап 17 — Веб-панель управления; бот выводится из проекта

**Причина**: клиент не может пользоваться ботом в Telegram. Бот при этом технически
исправен — `getMe` отвечает, `/start` обрабатывается, — но чинить нечего: проблема
на стороне клиента. Решение клиента: **бот не нужен вообще**, управление переезжает
в веб-панель.

**Цель**: `https://tolkay-voda.ru/admin` — постоянный адрес, вход по логину и паролю,
адаптив под телефон. Всё, что умел бот, плюс баннеры. После приёмки бот удаляется
из проекта.

### Вход

- **Логин и пароль**, постоянная ссылка `/admin` — одна и та же всегда, ничего
  одноразового и никаких токенов в URL.
- Пароль хранится хешем (`scrypt` из `node:crypto` — без новых зависимостей,
  в духе выбора `node:sqlite` вместо `better-sqlite3`).
- Таблица `admins`: `login`, `password_hash`, `created_at`. Первый аккаунт заводится
  из `.env` (`ADMIN_LOGIN` / `ADMIN_PASSWORD`) при первом старте.
- Сессия — httpOnly + Secure cookie, срок ~30 дней, «выйти» в меню.
- Смена пароля в самой панели.
- Ограничение попыток входа (защита от перебора), логи попыток на русском.

### Что переносим из бота

Полный список того, что бот умеет сегодня (по `src/bot/index.js`, 732 строки) —
переезжает один в один:

| Раздел | Что делает |
|---|---|
| Прогноз дня | сырой текст → выжимка через OpenAI → поля: вводка, вода, цвет, еда, совет, лунный день; фото необязательно; предпросмотр перед публикацией |
| Статья в блог | заголовок, текст, фото; slug генерируется |
| Песня в эфир | mp3 → S3 → запись в `tracks` → пересборка плейлиста и локального кэша |
| Правка прогноза | любое поле по отдельности, включая перегенерацию текста ИИ; удаление с подтверждением |
| Правка статьи | заголовок, текст, фото; удаление с подтверждением |
| Удаление песни | список, превью аудио, защита «нельзя удалить последнюю» |

Плюс два раздела, которых в боте нет:

- **Баннеры** — слот 1 и 2: картинка, ссылка, необязательный текст кнопки,
  очистить слот. Опирается на таблицу `ads` из этапа 16.
- **Список песен** нормальной таблицей, а не постранично по 8 кнопок.

### Вывод бота из проекта

Порядок важен: бот — единственный способ публиковать контент, пока панель не готова.

1. Панель написана и принята клиентом (все критерии ниже).
2. Только после этого: убрать сервис `bot` из `docker-compose.yml`, удалить
   `src/bot/`, вычистить `BOT_TOKEN` и `BOT_ADMIN_IDS` из `.env` и `config.js`,
   убрать зависимость `grammy` из `package.json`.
3. На сервере удалить `docker-compose.override.yml` — привязка `api.telegram.org`
   к `149.154.167.220` нужна была только боту.

**Побочный плюс**: вместе с ботом с сервера уходит единственная зависимость от
Telegram API. Блокировка Telegram у провайдера нового сервера (см. журнал этапа 15)
перестаёт что-либо значить — а это был главный технический довод против этого
сервера. Ссылки на канал в футере сайта открываются браузером посетителя и сервера
не касаются.

### Технические решения

- Панель — часть того же Fastify-приложения (`src/admin/`), отдельного сервиса
  и контейнера не заводим: лишний контейнер на сервере с одним ядром ни к чему.
- Слой данных общий (`src/lib/store.js`, `media.js`, `playlist.js`,
  `audio-cache.js`, S3). Логика публикации не переписывается заново — переносится
  то, что уже работает в боте.
- **Адаптив под телефон в первую очередь** — клиент правит контент с телефона.
  Формы в одну колонку, крупные поля ввода, загрузка файла с камеры и из галереи.
- Загрузка mp3 через форму без потолка Telegram в 20 МБ (можно поднять до ~50 МБ).
- `/admin` закрыт от индексации: `noindex` и `Disallow` в `robots.txt`,
  чтобы не сломать SEO с этапа 12.
- Мутации только POST с CSRF-токеном.
- Логи pino на русском: кто вошёл, что изменил.

**Критерии приёмки** (закрытие → tag `stage-17-done`):
- [ ] `https://tolkay-voda.ru/admin` открывается по постоянной ссылке, просит логин и пароль
- [ ] Неверный пароль не пускает; после серии попыток вход временно блокируется
- [ ] Пароль в базе лежит хешем, в открытом виде нигде не хранится
- [ ] Сессия живёт 30 дней; «выйти» завершает её
- [ ] Смена пароля из панели работает
- [ ] Прогноз дня: создание с выжимкой ИИ, правка каждого поля, удаление
- [ ] Статья: создание, правка, удаление; на сайте появляется по ЧПУ
- [ ] Песня: загрузка mp3 → появилась в эфире и в списке на сайте
- [ ] Удаление песни работает; последнюю удалить нельзя
- [ ] Баннеры: обе позиции ставятся и снимаются, на сайте меняются без передеплоя
- [ ] Панель нормально работает с телефона (проверено на реальном телефоне, не в эмуляторе)
- [ ] `/admin` не в поиске: `noindex` и `Disallow` на месте
- [ ] Клиент сам зашёл с телефона, опубликовал материал и поставил баннер
- [ ] **После приёмки клиентом**: бот удалён из compose, кода и зависимостей;
      сайт и эфир при этом работают
- [ ] `docker-compose.override.yml` с привязкой Telegram удалён с сервера

> **Объём.** Самый крупный этап после майлстоуна 1: своя авторизация, шесть экранов
> CRUD, загрузка файлов, вёрстка под телефон. По трудоёмкости сопоставим с этапами
> 10 и 13–14 вместе взятыми. Клиенту выставлять отдельной строкой, а не «мелкой
> правкой» — это второй интерфейс к приложению, а не перенос кнопок.

> **Отдать клиенту логин и пароль** нужно защищённым способом, и на первом входе
> заставить сменить пароль. Панель управляет всем содержимым сайта — это боевой
> доступ, а не демо.
